import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  placeholder: { backgroundColor: '#0f3460', alignItems: 'center', justifyContent: 'center' },
  label: { color: '#888', fontSize: 12 },
});
